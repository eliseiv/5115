# 100 — Known Tech Debt

Реестр осознанно принятого технического долга. Формат `TD-NNN`.

| ID | Описание | Причина | Влияние | Триггер закрытия |
|---|---|---|---|---|
| TD-040 | Файлы-знания workspace переинъектируются в контекст LLM на **каждом** ходе сессии с workspace ([ADR-064](adr/ADR-064-workspace-files-live-reinjection.md)): `context_for_session` перечитывает `workspace_files` из БД и повторно декодирует base64 картинок (`_image_block`) на каждый generation-запрос. | Живая переинъекция — единственная схема, сохраняющая инвариант «сырой base64 не персистится» ([ADR-020](adr/ADR-020-inline-base64-attachments-mvp.md)) и отсутствие утечки текста файлов в user-facing историю ([ADR-042](adr/ADR-042-hide-context-block-from-user-facing-history.md)) без нового strip. Токен-стоимость «файлы на каждом ходе» присуща самой фиче и не зависит от схемы. | Избыточная backend-работа (чтение `workspace_files` + повторный base64 картинок) на каждый ход; ограничена лимитами `WORKSPACE_FILE_MAX_COUNT`/`WORKSPACE_FILES_TOTAL_BYTES`/`WORKSPACE_CONTEXT_MAX_CHARS` ([ADR-036 §4/§6](adr/ADR-036-workspaces-implementation.md)). Prompt-cache не затронут (кэшируется только `system`). | При росте p95-латентности/нагрузки — кэш декодированных блоков в пределах запроса или пропуск повторной сборки при неизменном наборе файлов (по `updated_at`/хэшу). |
| TD-041 | **Формат content-блоков вложений и файлов-знаний собирается по провайдеру ИНСТАНСА, а не по фактическому провайдеру генерации** — латентный дефект, достижимый в byok cross-provider. Сборка блоков запроса-вложений (`prepare_attachments`, `src/app/chat/attachments.py`) и файлов-знаний workspace (`context_for_session`/`_build_file_attachments`, `src/app/workspaces/service.py`) в оркестраторе (`src/app/chat/orchestrator.py`) получает провайдера из `_active_provider()` (= `LLM_PROVIDER` инстанса, ADR-033). Но в mode=byok клиент генерации выбирается по провайдеру, **детектированному из ключа** (`detect_byok_provider`, `llm_client_for(...)`, [ADR-044 §2/§5.3](adr/ADR-044-multi-provider-byok.md)), который может отличаться от инстансного. На прод-инстансе с `LLM_PROVIDER=openai` пользователь с Anthropic-BYOK-ключом (`sk-ant-…`) получит блоки в OpenAI-форме (`input_image`/`input_text`/`input_file`), отправленные в Anthropic-клиент (ждёт `{type:image,source}`/`{type:text}`/`document`) → малформированные блоки → возможный сбой генерации (тот же класс, что исходный workspace-502, [ADR-064](adr/ADR-064-workspace-files-live-reinjection.md)); симметрично Anthropic-инстанс + OpenAI-ключ. Зона: **и** request-attachments, **и** workspace-файлы-знания (оба сайта построены на `_active_provider()`). Ортогонально [TD-024](100-known-tech-debt.md) (хранение payload) — это дефект **сборки** блоков перед отправкой. | Провайдер-агностичная валидация/лимиты вынесены ДО провайдер-ветки ([ADR-033 §5](adr/ADR-033-llm-provider-abstraction.md)), но выбор **формы** блока привязан к `_active_provider()` — на одно-провайдерном инстансе в mode=credits ([ADR-033](adr/ADR-033-llm-provider-abstraction.md)) это всегда совпадает с провайдером генерации, поэтому дефект не проявляется; расхождение возникает только на byok-пути ([ADR-044](adr/ADR-044-multi-provider-byok.md)), где провайдер берётся из ключа. При проектировании byok целевой провайдер до сборки блоков в эти два сайта не пробрасывался. | **Функциональный сбой генерации в достижимом сценарии** (byok cross-provider: инстанс одного провайдера + BYOK-ключ другого) — малформированные vision/document/text-блоки уходят в клиент чужого формата. Severity — **major** (функциональный отказ, не утечка/не двойной биллинг), но на текущем этапе novirell **pre-launch**: byok cross-provider маловероятен в проде (целевой дефолт — OpenAI-инстанс, [ADR-059](adr/ADR-059-openai-default-provider.md)), поэтому вероятность материализации низкая. Дефект реален и достижим (mode=byok доступен). Инвариант `_render_context_block`/persist ([ADR-020](adr/ADR-020-inline-base64-attachments-mvp.md)) не затронут — проблема только в форме блоков на wire. | Резолвить **целевой провайдер генерации ДО сборки блоков** (в byok — `detect_byok_provider(key)`, в credits — `_active_provider()`) и передавать его аргументом `provider` в `context_for_session`/`prepare_attachments` вместо `_active_provider()`; **либо** гейтить byok под провайдер инстанса (отклонять cross-provider-ключ на инстансе). Решение фиксируется новым ADR (расширяет [ADR-044](adr/ADR-044-multi-provider-byok.md)/[ADR-064](adr/ADR-064-workspace-files-live-reinjection.md)); реализация — backend. |
| TD-001 | Append-only `audit_logs` обеспечивается только на уровне приложения (нет DB-триггера/REVOKE на UPDATE/DELETE). | Простота на старте. | Теоретическая возможность изменить аудит при ошибке/злоупотреблении прямым доступом к БД. | При требованиях комплаенса — добавить REVOKE UPDATE/DELETE + write-only роль. |
| TD-002 | Контекст сессии для Claude реконструируется из `chat_steps` на каждый шаг (нет отдельного кэша собранного prompt). | Достаточно для старта; prompt caching Anthropic снижает стоимость. | Доп. чтение БД на каждый шаг tool-loop. | При росте латентности p95 — кэш собранного контекста в Redis. |
| TD-003 | Rate limiting и idempotency хранятся в Redis без репликации между регионами. | Single-region на старте. | При мульти-регионе потребуется пересмотр. | При выходе в multi-region. |
| TD-004 | Дефолтные rate limits и size-лимиты — эвристические (Q-003-1/Q-003-2), не калиброваны на реальной нагрузке. **Подпункт TD-004a (ADR-020, pypdf CPU-guard):** `_check_pdf_pages` (`src/app/chat/attachments.py`) парсит PDF через `pypdf.PdfReader` на декодированных байтах (до `ATTACHMENT_MAX_BYTES_DOCUMENT` = 8 MB) синхронно, без таймаута/изоляции. Структурно-злонамеренный PDF (раздутый/зацикленный xref, аномальная структура при малом числе страниц) способен вызвать CPU-spike в воркере на парсинге **до** проверки числа страниц. Риск ограничен: вход ≤ 8 MB (cap до парсинга), `ATTACHMENT_PDF_MAX_PAGES`=100 / `ATTACHMENT_MAX_COUNT`=10, парсинг только структуры (без полного рендера). Не эксплуатируется как amplification на MVP-объёме. | Нет прод-данных. Парсинг pypdf синхронный, в рамках лимитов размера/числа. | Возможны ложные `429`/`413` или недостаточная защита; **TD-004a:** локальный CPU-spike воркера на злонамеренном PDF (DoS-вектор ограничен 8 MB-cap и max_count=10). | После сбора прод-метрик откалибровать size/rate-лимиты. **TD-004a:** при появлении прод-метрик деградации на PDF-парсинге — обернуть `_check_pdf_pages` таймаутом/изоляцией (отдельный пул/процесс или `asyncio` timeout с прерыванием), либо ужесточить document-cap. |
| TD-005 | **Закрыт (2026-06-02), ревизован (2026-06-02 → [ADR-017](adr/ADR-017-shared-server-traefik-deploy.md)).** Финальная deploy-схема = **общий сервер за внешним Traefik + GitHub Actions SSH** (заменяет прежнюю VPS+Caddy-standalone). Сервис в `/opt/<service>` на общем Linux-сервере (`87.239.135.154`); reverse-proxy и TLS/ACME — внешний Traefik (`/opt/edge`), не наш стек. Наш `docker-compose`: `api` (`expose: 8000`, без `ports:` 80/443, сети `web` external + `default`, Traefik-labels), `postgres`/`redis` только в `default` без портов. Выкатка — GitHub Actions → SSH → `git pull --ff-only` + явные `build` / `run --rm migrate` / `up -d --no-build` + readiness-gate на сервере (образ собирается на сервере, не из registry; детали — [07-deployment.md §Процедура деплоя](07-deployment.md#процедура-деплоя-github-actions--ssh); GitHub Secrets `SSH_HOST`/`SSH_USER`/`SSH_PRIVATE_KEY`). Топология и процедура — [07-deployment.md §Топология](07-deployment.md#топология-mvp--общий-сервер-за-внешним-traefik) и [§Процедура деплоя](07-deployment.md#процедура-деплоя-github-actions--ssh). Не зависит от Q-002-1: KMS на MVP — local master key из env ([ADR-003](adr/ADR-003-byok-envelope-encryption.md)). | Closed→revised: владелец инфраструктуры предоставил общий сервер с уже работающим Traefik. | Платформа выкатки зафиксирована; deploy-процедура и rollback (`git checkout <prev-commit>` + rebuild) описаны в 07-deployment.md. Прежние Caddy/nginx/`deploy-vps.sh`-артефакты — legacy (не используются). | Закрыт фиксацией shared-server+Traefik-топологии ([ADR-017](adr/ADR-017-shared-server-traefik-deploy.md)). При вводе registry/immutable-tag, миграции на оркестратор (k8s) или облачный KMS — новый ADR. |
| TD-007 | `STOREKIT_TEST_MODE` — env-gated режим тестовой верификации StoreKit (HS256-JWS под `STOREKIT_TEST_SECRET`) вместо реальной Apple-подписанной транзакции и Apple root CA. Семантика — [09-e2e-testing.md §2](09-e2e-testing.md#2-storekit_test_mode--env-gated-режим-тестовой-верификации). | Реальная Apple sandbox-интеграция и Apple root CA недоступны в e2e/CI; нужен воспроизводимый путь для прогона активации подписки + grant кредитов end-to-end (ADR-006). | Дефолт `false` → prod fail-closed не меняется (реальная JWS-верификация). Test-mode активен только при `STOREKIT_TEST_MODE=true` И непустом `STOREKIT_TEST_SECRET`; при включении — WARNING в лог на старте. Риск случайного включения в prod минимизирован, но это осознанный test-only seam. | Интеграция реальной Apple App Store Server API sandbox (Q-007-1): подключить sandbox-endpoint и Apple sandbox root CA, после чего test-mode-ветку удалить. ETA — после закрытия Q-007-1 (выбор sandbox/prod posture App Store). |
| TD-008 | **Закрыт (2026-05-25).** `migrations/env.py` брал URL из `get_settings().database_url`, а не из переданного Alembic `context.config` (`sqlalchemy.url`). Исправлено: URL берётся из `context.config` с fallback на `get_settings().database_url`, если ключ пуст. | Хрупкость порядка тестов и невозможность прогнать миграции против произвольной БД, переданной через Alembic Config (e2e-БД / testcontainers). qa и reviewer отметили перед e2e. | До фикса миграции игнорировали URL из Alembic Config → ненадёжность при e2e/тестах с отдельной БД. | Закрыт реализацией [09-e2e-testing.md §3.1](09-e2e-testing.md#31-td-008-migrationsenvpy--sqlalchemyurl-из-alembic-config); подтверждён e2e (migrate-job прошёл, миграция 0002 применена, `/ready` db=ok). |
| TD-006 | Окно двойной бесплатной генерации в trial при гонке. Два параллельных первых `/v1/chat/run` (subscription=none, trial_used=false, mode=credits) могут оба пройти policy-allow до атомарного `mark_trial_used`, и оба сгенерировать бесплатный ответ. | Policy-allow вычисляется до генерации и до flip `users.trial_used`; flip атомарен и идемпотентен, но не блокирует параллельную проверку (нет advisory lock на user при trial-allow). | Не биллинговая утечка: flip и списание НЕ дублируются (атомарный `UPDATE ... WHERE trial_used=FALSE`, idempotency-ledger, ADR-005). Максимум — 1 лишняя бесплатная генерация на пользователя при точной гонке двух первых запросов. Стоимость пренебрежимо мала. См. ADR-002 §Trial concurrency. | Если злоупотребление trial станет заметным по метрикам — взять `pg_advisory_xact_lock(user_id)` (или `SELECT ... FOR UPDATE` по `users`) на ветке trial-allow в `/chat/run`, сериализуя первый trial на пользователя. |

| TD-009 | Контент файлов сайта (`site_files.content`) хранится в БД (BYTEA), а не в object-storage. | Простота на старте; транзакционность с tool-операциями; без новой инфраструктуры ([ADR-010](adr/ADR-010-backend-hosted-preview.md)). | При больших/многочисленных проектах раздувает БД и нагружает её отдачей статики. Ограничено лимитами (файл ≤ 1MB, проект ≤ 10MB, ≤ 200 файлов). | При росте объёма/нагрузки — вынести контент в S3-совместимое object-storage (+ CDN), сохранив контракты `site.*`/preview. Интерфейс Website Service спроектирован под замену backend'а хранения. |

| TD-010 | Orphan-вложения (`attachments` с `session_id IS NULL`, не привязанные к сессии) не очищаются фоновым джобом. **Не актуален для MVP** ([ADR-020](adr/ADR-020-inline-base64-attachments-mvp.md)): таблицы `attachments` на MVP нет (inline base64 не персистится), orphan-вложений не возникает. Применимо только при реализации двухшаговой модели (**TD-015**). | Простота на старте; нет планировщика. | Накопление неиспользованных загрузок в БД (BYTEA) — **только** в двухшаговой модели. | Добавить фоновую очистку (`attachments` старше `ATTACHMENT_ORPHAN_TTL`, дефолт 24h, без `session_id`) при появлении планировщика/cron-инфраструктуры **и** реализации двухшаговой модели ([ADR-014](adr/ADR-014-multimodal-attachments.md), **TD-015**). |
| TD-011 | Отправка push-уведомлений (APNs-клиент, триггеры доставки) не реализована — на старте только хранение настройки (`user_preferences.notifications_enabled`, дефолт `false` — [ADR-032](adr/ADR-032-notifications-enabled-default-false.md), private-by-default) и регистрация device-токена (`device_push_tokens`). | Объём APNs-интеграции (сертификаты/JWT APNs, ретраи, триггеры событий) велик; ядро приложения приоритетнее. | Toggle и регистрация токена работают, но фактических push нет. UI-настройка функциональна, доставка — нет. | Реализовать APNs-клиент (token-based JWT auth), привязать к событиям (например завершение длинной генерации), env `APNS_*`. Вынести в отдельный спринт. См. [modules/notifications/00-overview.md](modules/notifications/00-overview.md). |
| TD-012 | N+1 на `preview` в списке чатов: `ChatsRepository.list_chats` выполняет отдельный index-backed запрос `_preview()` для каждого чата страницы (до `limit`+1 строк), а не одним запросом. См. [modules/chats/02-api-contracts.md](modules/chats/02-api-contracts.md). | Простота на старте; каждый `_preview()` — точечный индексированный select последнего шага сессии; масштаб per-user (страница ≤ 100) делает накладные расходы пренебрежимыми. | До `limit` дополнительных коротких запросов на страницу списка чатов. Приемлемо для текущего per-user масштаба. | При росте размера страниц/нагрузки — заменить per-row `_preview()` на один запрос с LATERAL join или window-функцией (`ROW_NUMBER() OVER (PARTITION BY session_id ...)`), сохранив контракт ответа `GET /v1/chats`. |

| TD-013 | Истёкшие/использованные/отозванные `auth_refresh_tokens` не очищаются фоновым джобом. | Простота на старте; нет планировщика (как TD-010). | Накопление неактивных строк refresh-токенов. Малый объём (по строке на rotation), не влияет на корректность (валидность проверяется по `used_at`/`revoked_at`/`expires_at`). | Добавить фоновую очистку строк с `expires_at < now()` или непустым `used_at`/`revoked_at` старше grace-периода при появлении планировщика/cron ([ADR-018](adr/ADR-018-embedded-auth-issuer.md), [modules/auth/04-data-model.md](modules/auth/04-data-model.md)). |

| TD-014 | Anthropic upstream-ошибки логируются как generic «upstream error» без `status_code`, `error.type`, `error.message` и `request_id`. `src/app/chat/anthropic_client.py` при `anthropic.APIStatusError` (а также `APITimeoutError`/`APIConnectionError`/`AuthenticationError`) мапит в `UpstreamError("anthropic upstream error")`, **проглатывая тело и статус** исходной ошибки. | Минимальная обработка ошибок на старте: ловим исключение SDK и сразу обобщаем в доменный `UpstreamError`. | **Маскировка диагностики**: реальная причина отказа не видна в логах. Подтверждено на проде — Anthropic вернул `400 invalid_request: "This organization has been disabled."`, но в логах был только generic «upstream error» → qa ошибочно списал на старый SDK и предложил bump (ложный фикс); истинная причина найдена только прямой диагностикой в контейнере. Поведение наружу корректно (502), но MTTR диагностики высок. | Реализовать **контракт логирования upstream-ошибок Anthropic** — см. [modules/chat-orchestrator/03-architecture.md §Логирование upstream-ошибок Anthropic](modules/chat-orchestrator/03-architecture.md#логирование-upstream-ошибок-anthropic-td-014). Логировать тело ошибки апстрима (поля-источники `error.type`/`error.message`/`request_id` + `status_code`/`model`) под camelCase лог-ключами `status_code`/`errorType`/`errorMessage`/`anthropicRequestId`/`model`/`exceptionClass` (событие `anthropic_upstream_error`) по матрице уровней; api-key и user-content — под redaction (см. [05-security.md §Логирование](05-security.md#логирование-безопасное)). Error-contract наружу (502) **не меняется**. Scope backend. |

| TD-015 | Двухшаговая модель вложений (отдельный `POST /v1/attachments` upload-эндпоинт + таблица `attachments` + retention/orphan-cleanup) **не реализуется** на MVP — мультимодальный ввод реализован inline base64 в `/v1/chat/run` ([ADR-020](adr/ADR-020-inline-base64-attachments-mvp.md)). | Inline проще для MVP (нет нового хранилища/lifecycle, один round-trip); двухшаговая модель оправдана только при переиспользовании вложений между сообщениями, очень больших файлах или object-storage. | Нет reuse вложений; нет постоянного хранения загруженного контента (вложение живёт только в рамках первого обращения к модели); каждый запрос с вложением несёт его base64 в теле. Приемлемо для сценариев дизайна (одноразовый ввод к сообщению). | При появлении требований reuse/больших файлов/object-storage — реализовать двухшаговую модель по [ADR-014](adr/ADR-014-multimodal-attachments.md) (сохранён как зафиксированный путь): upload-эндпоинт, таблица `attachments`, TD-010-аналог retention. Тогда же активируется ранее заведённая ветка хранения байтов (TD-009). |
| TD-016 | `anthropic 0.39.0` не типизирует `document`-блок (есть `ImageBlockParam`, нет `DocumentBlockParam` — проверено эмпирически). PDF-`document`-блок передаётся как сырой dict (`cast(Any, messages)`), без типобезопасности/валидации на стороне SDK. **Открыт; включает обязательное live-подтверждение:** wire-совместимость PDF-`document`-блока на текущей версии SDK подтверждается только e2e с реальным Anthropic ([06-testing-strategy.md](06-testing-strategy.md) §Тест-кейсы вложений, строка E2E), который **на 2026-06-03 не выполнен — org Anthropic отключена** (см. memory/deployment-state). Прогон обязателен сразу после восстановления org. | SDK старый (late-2024); bump SDK — отдельное решение с риском регрессий в существующем tool-loop/usage-parsing. На MVP dict-passthrough работает (wire-совместимость подтверждается e2e с реальным Anthropic). | Нет статической типизации/SDK-валидации `document`-блока; при изменении формата document-блока в будущих моделях рассинхрон выявится только в рантайме/e2e. image-блок типизирован (`ImageBlockParam`), document — нет. | Bump `anthropic` до версии с `DocumentBlockParam` (и betas, если потребуется) при необходимости типобезопасности или при отказе API на текущем формате; перепроверить tool-loop/usage-parsing. Решение об апгрейде SDK фиксируется отдельным изменением [02-tech-stack.md](02-tech-stack.md). |

| TD-017 | `SizeLimitMiddleware` (`src/app/api_gateway/middleware.py`) отклоняет тело по заголовку `Content-Length` **до** парсинга. При отсутствии заголовка (`Transfer-Encoding: chunked` или клиент не прислал `Content-Length`) проверка размера **пропускается** — тело проходит к парсингу без transport-level guard'а (включая повышенный лимит роута `/v1/chat/run`, ADR-020). | Простота на старте; за внешним Traefik ([ADR-017](adr/ADR-017-shared-server-traefik-deploy.md)) клиентский трафик нормализуется, реальные клиенты iOS/Swagger шлют `Content-Length`. На MVP не эксплуатируется. | Теоретический обход transport-лимита chunked-запросом без `Content-Length`: тело читается до прикладной валидации размеров (которая всё равно сработает в схеме/orchestrator до декодирования base64, но уже после буферизации тела). Не утечка, но guard не streaming-устойчив. | Реализовать streaming body-guard, считающий фактически прочитанные байты независимо от `Content-Length` (отклонять при превышении на лету), **либо** документально подтвердить, что внешний Traefik нормализует/требует `Content-Length` (или режет chunked) на edge. Связан с калибровкой лимитов (TD-004). |

| TD-018 | **Оба провайдера — оркестрационный вызов non-streaming.** Anthropic (`anthropic_client.create_message`) — non-streaming при `ANTHROPIC_MAX_TOKENS=16000` ([ADR-025](adr/ADR-025-parallel-tool-calls-and-max-tokens-truncation.md)); **OpenAI (`openai_client` после спринта 1) — тоже non-streaming: `client.responses.create` без стриминга при `OPENAI_MAX_TOKENS=16000` и `OPENAI_TIMEOUT_SECONDS=120`** ([ADR-059](adr/ADR-059-openai-default-provider.md), паритет с anthropic-путём). Прогрессивная отдача токенов и partial-tool_use accumulation отсутствуют у обоих; обрезка по лимиту обрабатывается как `blockReason=max_tokens` (повтор/сокращение запроса), без «дописать с места» (Anthropic `stop_reason=max_tokens`; OpenAI `status="incomplete"` + `incomplete_details.reason="max_output_tokens"`, [ADR-059 §1](adr/ADR-059-openai-default-provider.md)). | Минимальная дельта на MVP: не трогать tool-loop/usage-parsing ни на одном провайдере; `16000` ниже порога non-streaming-гарда SDK, латентность приемлема. | Очень длинная генерация (близко к output-лимиту) блокирует ответ до полного завершения вызова (нет инкрементального стрима в UI) — **на обоих провайдерах**; при превышении лимита — `max_tokens`-blocked вместо продолжения. `ANTHROPIC_TIMEOUT_SECONDS`/`OPENAI_TIMEOUT_SECONDS=120` страхуют от ложного 502. | При требовании прогрессивной отдачи в UI **или** дальнейшем росте `*_MAX_TOKENS` за порог non-streaming-гарда — перейти на streaming (**обоих** клиентов: Anthropic `messages.stream`, OpenAI `responses.create(stream=True)`/`responses.stream`) + аккумуляция блоков (включая partial tool_use / reasoning-items), пересмотреть tool-loop/usage-parsing. Триггер закрытия покрывает **оба** клиента. Решение фиксируется обновлением [02-tech-stack.md](02-tech-stack.md) и новым/расширенным ADR. |

| TD-022 | При пустом `SERVICE_DOMAIN` (локальная разработка) `site.preview` отдаёт **относительный** preview-URL (`/v1/preview/...`), а не абсолютный ([ADR-031](adr/ADR-031-absolute-preview-url.md)). | Локально публичный хост неизвестен; хардкод `localhost`/порта хрупок и ломал бы dev/offline-тесты. На проде `SERVICE_DOMAIN` всегда задан → абсолютный URL. | В dev-окружении модель снова может «достроить» хост к относительному пути (как было на проде до фикса). Не влияет на прод (домен задан) и на безопасность (авторизация в подписи). | При необходимости абсолютного URL в dev — задать `SERVICE_DOMAIN` локально (напр. `localhost:8000`) или ввести dev-only базовый URL. На проде долг не материализуется. |
| TD-020 | **Consumable-пакеты токенов НЕ через Adapty** в текущей итерации ([ADR-029](adr/ADR-029-adapty-subscription-webhook.md)). Adapty-вебхук покрывает только 4 события подписки; покупка пакетов токенов остаётся на `/v1/tokens/purchase` (StoreKit consumable IAP, [ADR-015](adr/ADR-015-consumable-token-iap.md)). | Scope-ограничение итерации: подписки — приоритет; consumable-события Adapty требуют отдельного проектирования idempotency и policy-guard. | Два разных пути пополнения кредитов (подписка → Adapty; разовые пакеты → StoreKit). Приемлемо: пути не пересекаются по продуктам. | При решении переносить consumable на Adapty ([Q-029-1](99-open-questions.md)) — спроектировать consumable-события Adapty + idempotency (по аналогии с `adapty_webhook_events`) + сохранить policy-guard «требует подписки» ([Q-015-1](99-open-questions.md)). |
| TD-021 | **`/v1/subscription/sync` (StoreKit JWS) НЕ ретирован** — оставлен рабочим параллельно с Adapty-вебхуком ([ADR-029](adr/ADR-029-adapty-subscription-webhook.md)), хотя источник истины подписок сместился на Adapty. | Не ломать существующий путь до подтверждения, что весь трафик идёт через Adapty; безопасный переходный период. | **Риск двойного начисления:** один путь использует `idempotency_key=sub-grant:{transactionId}`, другой — `adapty-txn:{transaction_id}` ([ADR-047](adr/ADR-047-adapty-real-payload-format-and-grant-idempotency.md)); ledger-idempotency между путями НЕ срабатывает (разные префиксы). Митигация на MVP — **контрактная** (клиент использует ОДИН путь подписок, [ADR-029](adr/ADR-029-adapty-subscription-webhook.md), [05-security.md](05-security.md)), не кодом. | После периода наблюдения подтвердить отсутствие StoreKit-`sync`-трафика по подпискам → ретировать `sync` (удалить роут/код подписочной ветки) либо ввести cross-path idempotency-мост ([Q-029-2](99-open-questions.md)). |
| TD-019 | **Закрыт (2026-06-10), вариант A.** Ранее: tz-база (zoneinfo) отсутствовала в runtime-образе → аргумент `tz` инструмента `time.now` не работал в prod (slim-образ `python:3.12-slim-bookworm` без системной tz-базы и без Python-пакета `tzdata` в зависимостях). **Решено:** devops добавил pure-Python зависимость `tzdata` в зависимости проекта (`pyproject.toml` — `tzdata>=2024.1`; `uv.lock` — `tzdata 2026.2`), без правки Dockerfile (вариант A — pure-Python, без системных пакетов, предпочтительно для slim). В собранном prod-образе `zoneinfo.ZoneInfo("Europe/Moscow")` резолвится; аргумент `tz` работает, локальные `local`/`timezone` возвращаются. UTC-набор результата (`utc`/`unix`/`weekday`) от tz-базы не зависел и работал всегда. | Закрыт добавлением tz-базы как pure-Python зависимости `tzdata` (вариант A). | tz-база гарантирована в runtime-образе → `tz` (`Europe/Moscow` и пр.) штатно отдаёт локальное время. Невалидная/мусорная зона по-прежнему деградирует к tool-result error `invalid_timezone` (ход не падает) — это нормальное поведение контракта, не дефект ([ADR-026 §6](adr/ADR-026-global-server-side-tools-and-time-now.md)). | Закрыт добавлением `tzdata` в зависимости проекта (вариант A, [ADR-026 §10](adr/ADR-026-global-server-side-tools-and-time-now.md)). При переходе на иной runtime-образ — перепроверить наличие tz-базы. |

| TD-023 | **Закрыт (2026-06-19, [ADR-041](adr/ADR-041-openai-native-pdf-attachment.md)).** Ранее: PDF-вложения не поддерживались на OpenAI-инстансах — при `provider=openai` `type: document` (`application/pdf`) → `ValidationFailedError` (`422 unsupported_media_type`); `image`/`text` работали. **Решено:** OpenAI теперь принимает PDF на вход (vision-модели gpt-4o+), поэтому в openai-ветке маппинга (`src/app/chat/attachments.py::_openai_content_block`, `document`) `application/pdf` больше **НЕ** `422`. Основной путь — OpenAI Chat Completions content-часть `file` (`{"type":"file","file":{"filename","file_data":"data:application/pdf;base64,..."}}`, точный wire-shape backend сверяет с установленным `openai` SDK 1.109.1); гарантированный фолбэк (если SDK/endpoint не принимает `file`-часть) — серверное извлечение текста PDF через `pypdf` (паттерн `workspaces/text_extract.py`) → text-блок. Anthropic-ветка не трогалась; валидация/лимиты (magic-bytes, page-guard ≤100, doc ≤8 MB) не менялись (в пределах OpenAI 100стр/32MB); сырой base64 не персистится; биллинг неизменен. | OpenAI ранее не принимал PDF в Chat Completions vision; на момент ADR-033 backend-предобработка была вне scope провайдер-абстракции. | Контракт `/v1/chat/run` теперь единообразен: PDF принимается на обоих провайдерах ([05-security.md §Провайдер LLM: OpenAI](05-security.md#провайдер-llm-openai-adr-033), [API-REFERENCE.md §Вложения](API-REFERENCE.md)). | Закрыт реализацией [ADR-041](adr/ADR-041-openai-native-pdf-attachment.md) (native file-input или text-extraction фолбэк). Богатый PDF-разбор через растеризацию/OpenAI Responses API при потребности — [Q-033-2](99-open-questions.md). |
| TD-024 | **`chat_steps.payload` хранит wire-формат активного провайдера**, а не нейтральный (провайдер-агностичный) формат ([ADR-033 §3](adr/ADR-033-llm-provider-abstraction.md)). Кросс-провайдерный реплей в одной БД невозможен. | Инстанс одно-провайдерный — реплей всегда через тот же клиент, что писал payload; нейтральный формат хранения добавил бы двунаправленную neutral↔wire-трансляцию на каждый реплей без выгоды на MVP. | Нельзя реплеить диалог, начатый на одном провайдере, на другом провайдере в той же БД (миграция данных между провайдерами / A/B одного диалога недоступны). Для текущей цели (отдельные инстансы на провайдер) ограничение не материализуется. | При требовании кросс-провайдерного реплея — спроектировать нейтральный формат хранения `chat_steps.payload` + двунаправленную трансляцию neutral↔wire в каждом клиенте (отдельный ADR, [Q-033-1](99-open-questions.md)). |
| TD-026 | **Пресеты промтов нельзя менять без деплоя backend** ([ADR-035](adr/ADR-035-prompt-presets-endpoint.md)): набор/тексты/иконки пресетов `GET /v1/presets` заданы статическим реестром в коде (`src/app/chat/presets.py`). Правка состава = коммит + деплой backend. | На старте — простота, версионирование, review/diff текстов, единый набор на всех 3 инстансах ([ADR-035 §2](adr/ADR-035-prompt-presets-endpoint.md)). Целевое требование («менять без релиза iOS») уже выполнено (пресеты отдаёт backend); правка без релиза **iOS** ≠ правка без деплоя **backend**. | Изменить пресеты нельзя оперативно без CI/CD-деплоя. На текущем масштабе (7 статичных пресетов, редкие изменения) приемлемо. | Промежуточный шаг — config-JSON env (`PROMPT_PRESETS`, парсинг по образцу `token_products()`/`allowed_models()` в [config.py](../src/app/config.py)), правка `.env` + рестарт без пересборки образа; следующий — таблица БД + admin-CRUD ([Q-035-1](99-open-questions.md)). Публичный контракт `GET /v1/presets` при любом переходе не меняется (источник инкапсулирован за роутером). |
| TD-028 | **`context` (per-message, [ADR-037](adr/ADR-037-chatrunrequest-context-allowlist-injection.md)) и `preferences.code_defaults` (серверные дефолты) не связаны.** `context` (язык кода/стиль/тон/локаль на ход) инъектируется в user-сообщение, но НЕ читает/не накладывается на серверные дефолты пользователя из `preferences`; нет правила приоритета (per-message override vs server default) и нет авто-подстановки `code_defaults` когда `context` пуст. Allowlist `context` — статический реестр в коде → расширение требует деплоя backend ([Q-037-1](99-open-questions.md)). | Поставка ADR-037 — атомарный scope: сделать `context` функциональным (per-message, без БД-миграции). Слияние с `preferences.code_defaults` (источник дефолтов, правило приоритета, идемпотентность семантики) — отдельное проектирование, вне scope. | Пользователь, задавший дефолты в preferences, всё равно обязан слать `context` на каждый ход, чтобы они применились (нет «липкости» через серверные дефолты). На текущем масштабе приемлемо (iOS может подставлять свои дефолты в `context` на клиенте). | Спроектировать слияние `context` ⊕ `preferences.code_defaults` (правило: per-message `context` перекрывает server default; при пустом ключе — подстановка из preferences) + источник `code_defaults` в preferences-модуле. Решение фиксируется новым/расширенным ADR ([Q-037-1](99-open-questions.md)). |
| TD-025 | **Биллинг не дифференцирован по модели** ([ADR-034](adr/ADR-034-user-model-selection.md)): выбор любой модели из allowlist списывает одинаково — 1 кредит = 1 сообщение ([ADR-006](adr/ADR-006-credit-billing-and-subscription-grant.md)). Премиум-модель (дороже у провайдера) тарифицируется как базовая. | Поставка 1 (выбор модели) — атомарный scope: дать выбор и зафиксировать за сессией. Тарифные коэффициенты per-model требуют отдельного проектирования (источник коэффициентов, идемпотентность переменного списания) и продуктового решения по ценам. | Возможен дисбаланс себестоимости: пользователи выбирают самую дорогую модель при той же цене в кредитах. На текущем масштабе и при операторском контроле allowlist (оператор кладёт в env только приемлемые по стоимости модели) приемлемо. | Ввести стоимость per-model (коэффициент в allowlist-объекте или отдельный env `MODEL_CREDIT_COST`) + переменное списание в orchestrator с сохранением идемпотентности ([ADR-005](adr/ADR-005-idempotency-ledger.md)). Решение фиксируется новым ADR ([Q-034-1](99-open-questions.md)). |

> **BUG-A (max_tokens truncation, закрыт дизайном, не TD):** молчаливая потеря обрезанных `tool_use` при `stop_reason="max_tokens"` (ход уходил в else-ветку → `assistant_message`/`toolCall=null`) **устранена** явной обработкой `stop_reason` ([ADR-025](adr/ADR-025-parallel-tool-calls-and-max-tokens-truncation.md): `blockReason=max_tokens`, кредит не списывается) + поднятием дефолта `ANTHROPIC_MAX_TOKENS` до 16000. Баг-фикс, не долг. Остаточный non-streaming-долг — **TD-018**.
>
> **BUG-B (single toolCall на parallel tool use, закрыт дизайном, не TD):** потеря всех client-side `tool_use` кроме первого (`first_client_out`) → orphan `tool_use` на continuation → Anthropic 400/502 **устранена** возвратом всех вызовов в `toolCalls[]` + батч `/chat/tool-result` + барьер хода ([ADR-025](adr/ADR-025-parallel-tool-calls-and-max-tokens-truncation.md)). Баг-фикс, не долг.

> **BUG-5 (закрыт дизайном, не TD):** ненадёжность `chat_steps.created_at` как порядкового ключа реконструкции (transaction-time `now()` одинаков для шагов одной транзакции; UUID-tie-break случаен → orphan tool_result на server-side tool-loop → Anthropic 400/502) **устранена** введением монотонной колонки `chat_steps.seq` ([ADR-021](adr/ADR-021-deterministic-step-order-and-block-normalization.md), миграция `0006`). Это баг-фикс (восстановление корректности), а не принятый долг — TD не заводится. Реализация — backend ([modules/chat-orchestrator/03-architecture.md §Детерминированный порядок шагов](modules/chat-orchestrator/03-architecture.md#детерминированный-порядок-шагов-и-нормализация-payload-adr-021)). Заодно нормализуются content-блоки перед персистом (служебное SDK-поле `caller` вырезается) — тоже фикс, не долг.

> TD-009 (контент в БД vs object-storage) на MVP покрывает `site_files.content` (website-builder). Хранение байтов вложений **на MVP не возникает** — inline base64 не персистится ([ADR-020 §3](adr/ADR-020-inline-base64-attachments-mvp.md), сырой base64 не пишется в `chat_steps.payload`). Прежняя пометка про `attachments.content` относится к двухшаговой модели ([ADR-014](adr/ADR-014-multimodal-attachments.md)), которая отложена (**TD-015**); при её реализации TD-009 снова охватит байты вложений.

| TD-027 | **Байты файлов-знаний workspace (`workspace_files.content`) хранятся в БД (BYTEA), а не в object storage** ([ADR-036 §4](adr/ADR-036-workspaces-implementation.md)). | Простота на старте; транзакционность с CRUD; без новой инфраструктуры (тот же выбор, что [TD-009](100-known-tech-debt.md) для `site_files`). Хранение в `attachments`/object-storage отвергнуто — фича не должна зависеть от отложенного `attachments` ([TD-015](100-known-tech-debt.md)). | При больших/многочисленных файлах раздувает БД и нагружает её при подаче контекста. Ограничено лимитами (`WORKSPACE_FILE_MAX_BYTES`=8 MB/файл, `WORKSPACE_FILES_TOTAL_BYTES`=32 MB/workspace, `WORKSPACE_FILE_MAX_COUNT`=20). | При росте объёма/нагрузки — вынести `content` в S3-совместимое object storage (+ CDN при необходимости), сохранив контракты `/v1/workspaces/{id}/files` и подачу контекста; `extracted_text` может остаться в БД (нужен для инъекции без чтения байтов). Симметрично [TD-009](100-known-tech-debt.md). |

| TD-029 | **Легаси `byok_keys`-строки с `provider=NULL`** ([ADR-044](adr/ADR-044-multi-provider-byok.md)): миграция `0013` добавляет колонку `provider` без backfill, поэтому BYOK-ключи, сохранённые до миграции, имеют `provider=NULL`. Для таких строк провайдер на генерации определяется fallback-детектом по расшифрованному ключу (`detect_byok_provider(plaintext)`), а `activeModel` на чтениях `/v1/byok` (где ключ намеренно не расшифровывается) деградирует к BYOK-дефолту **активного** инстанса вместо провайдера ключа. | Expand-only без backfill (симметрия с `0009`/`0010`); backfill потребовал бы массовой расшифровки всех ключей (расширение поверхности plaintext, [ADR-003](adr/ADR-003-byok-envelope-encryption.md)) либо знания провайдера, которого нет в строке. | Генерация byok для легаси-строк корректна (fallback-детект). Возможна неверная `activeModel` в ответе `/v1/byok` для до-миграционного OpenAI-ключа на Anthropic-инстансе (и наоборот) до следующего `set`. Узкое окно: только легаси-строки, только до следующего `set_key` (который пишет `provider`). | Закрывается естественно при следующем `set_key` пользователя (колонка заполняется детектором). При необходимости — одноразовый backfill-джоб с расшифровкой под контролируемой процедурой (отдельное решение), либо ленивая запись `provider` при первой генерации легаси-строки. |

| TD-030 | **Новые режимы диалога работают только на OpenAI-инстансах** ([ADR-055](adr/ADR-055-dialog-mode.md)/[ADR-059](adr/ADR-059-openai-default-provider.md)): `deep_thinking`/`study_learn`/`search` provider-gated на OpenAI (Anthropic-путь не несёт Responses-механизмов — reasoning-replay, встроенный `web_search`-tool, strict-quiz одним ходом). На anthropic-инстансе эти режимы → `422 unsupported_dialog_mode`; `smart` работает на обоих. **Генерация изображений ([ADR-058](adr/ADR-058-image-generation.md)) — НЕ часть этого долга:** она **не** привязана к `LLM_PROVIDER`, а гейтится наличием `OPENAI_API_KEY` (генератор — отдельный OpenAI-клиент), поэтому доступна и на anthropic-**чат**-инстансе с настроенным ключом ([ADR-058 §3](adr/ADR-058-image-generation.md)). | Паритет потребовал бы переписать `AnthropicClient` под extended-thinking/провайдерский поиск/строгий tool-контракт — вне scope этой поставки; фича не должна блокироваться переписыванием обоих клиентов. | Продвинутые режимы и картинки недоступны на anthropic-инстансах; продукт с полным набором Modes = OpenAI-инстанс. Приемлемо: целевой дефолт продукта — OpenAI ([ADR-059](adr/ADR-059-openai-default-provider.md)), Anthropic остаётся для BYOK/совместимости. | При потребности паритета — реализовать режимы на Anthropic (extended thinking, интеграция веб-поиска, tool-квиз) отдельным ADR; provider-gate снимается по мере покрытия. |
| TD-031 | **Байты сгенерированных изображений (`generated_images.content`) хранятся в БД (BYTEA), а не в object storage** ([ADR-058](adr/ADR-058-image-generation.md)). В один ряд с [TD-009](100-known-tech-debt.md) (`site_files`) / [TD-027](100-known-tech-debt.md) (`workspace_files`): контент в БД вместо S3. **Уточнение (ADR-058 §6):** TTL/retention для изображений **временного** чата теперь **есть** (`expires_at`, логическая недоступность безусловна); для изображений **обычного** чата retention отсутствует (живут бессрочно, `expires_at=NULL`); гарантированный периодический физический GC — best-effort ([TD-036](100-known-tech-debt.md)). Данный TD сужен до **object storage** (BYTEA→S3). | Простота на старте; транзакционность с ходом (consume→INSERT, [ADR-058 §5](adr/ADR-058-image-generation.md)); без новой инфраструктуры. | Накопление изображений в БД (BYTEA раздувает базу и нагружает отдачей байтов). Для обычного чата картинки не истекают; для временного — истекают по TTL ([TD-036](100-known-tech-debt.md) — физический sweep best-effort). Ограничено платностью генерации (`IMAGE_CREDITS_COST` сдерживает объём). | При росте объёма/нагрузки — вынести `content` в S3-совместимое object storage (+ CDN), сохранив контракт `GET /v1/images/{id}` и семантику `expires_at`. Retention для обычного чата (если понадобится) — отдельным решением. Симметрично [TD-009](100-known-tech-debt.md)/[TD-027](100-known-tech-debt.md). |
| TD-032 | **У временного чата нет кросс-запросной идемпотентности дебета** ([ADR-056](adr/ADR-056-temporary-chat.md)): `messageStepId` минтится на каждый HTTP-запрос и не персистится (ход не сохраняется в БД), поэтому повторный идентичный запрос спишет кредит заново. В пределах одного запроса ledger-идемпотентность ([ADR-005](adr/ADR-005-idempotency-ledger.md)) работает штатно. | **By design:** у временного чата нет континьюации и нет persisted-хода, к которому можно привязать стабильный ключ; персист ключа противоречил бы «ничего не хранится» ([ADR-056 §1](adr/ADR-056-temporary-chat.md)). | Клиент, ретраящий один и тот же временный ход (сетевой сбой), может списать кредит дважды. Приемлемо: временный чат — нишевый приватный режим; обычный (persisted) путь идемпотентен по `messageStepId`. | При потребности — вынести in-flight идемпотентность-метку временных ходов в Redis (TTL-ключ по хешу запроса), не нарушая инвариант «не персистить в БД». Отдельное решение при появлении реальной проблемы двойного списания. |

| TD-033 | **Нет fail-fast детекта старой (Chat Completions) формы `chat_steps.payload` в Responses-`_build_input`** ([ADR-059](adr/ADR-059-openai-default-provider.md), спринт 1). После перехода `OpenAIClient` на Responses API реплей истории строит `input` из `chat_steps.payload`. Если на OpenAI-инстансе окажется payload **старой** формы (Chat Completions `message`/`tool_calls`, а не Responses items), `_build_input` отдаст его в `responses.create` **дословно** → OpenAI `400` → непрозрачный `502` без диагностики (не видно, что причина — устаревшая форма payload). Ортогонально [TD-024](100-known-tech-debt.md) (кросс-провайдерный реплей невозможен — payload хранит wire активного провайдера) и [Q-059-1](99-open-questions.md) (одноразовая **пред-мержевая проверка** отсутствия таких сессий). | [Q-059-1](99-open-questions.md) закрывает риск на пред-мерж (прод на `LLM_PROVIDER=anthropic` → OpenAI-сессий со старым payload быть не должно), но это **операционная проверка**, а не рантайм-защита. Смена `LLM_PROVIDER=anthropic→openai` на **работающем** инстансе (переиспользование той же БД, контекст stale-model фолбэка [ADR-044](adr/ADR-044-multi-provider-byok.md)) или ручная миграция данных всё ещё способны привнести чужую форму payload — и тогда падение непрозрачно. | Узкое окно (одно-провайдерный инстанс [ADR-033](adr/ADR-033-llm-provider-abstraction.md), прод на anthropic); при материализации — `502` без диагностики, высокий MTTR (симметрично [TD-014](100-known-tech-debt.md) для Anthropic upstream-ошибок). Не утечка, не двойной биллинг. | Дешёвый shape-check формы item в `_build_input` (Responses ждёт items с `type` ∈ {`message`,`function_call`,`function_call_output`,`reasoning`,…}; форма Chat Completions (`role`+`content`/`tool_calls` без `type`-item) → **явная диагностируемая ошибка** `stale_payload_format` в лог + доменный `UpstreamError`, а не сырой `400`→`502`), **либо** адаптер чтения старого payload ([ADR-059 §8](adr/ADR-059-openai-default-provider.md)). Закрытие независимо от [Q-059-1](99-open-questions.md) (та — про пред-мержевую проверку данных, эта — про рантайм-диагностику). |

| TD-034 | **Reasoning-токены Deep Thinking делят общий output-бюджет `OPENAI_MAX_TOKENS` с самим ответом** ([ADR-059 §3,§4](adr/ADR-059-openai-default-provider.md); extends [TD-018](100-known-tech-debt.md)). При `dialog_mode=deep_thinking` reasoning-токены модели расходуют тот же `max_output_tokens` (= общий `OPENAI_MAX_TOKENS`, дефолт 16000), что и видимый ответ — **отдельного бюджета на reasoning нет**. При `DEEP_THINKING_EFFORT=high` (поддерживаемое значение конфига) reasoning может съесть бюджет до генерации видимого ответа → `status="incomplete"` / `incomplete_details.reason="max_output_tokens"` → пользователь получает `blocked(max_tokens)` вместо ответа ([ADR-059 §1](adr/ADR-059-openai-default-provider.md)). Это **reasoning-специфичная амплификация** проблемы TD-018, отдельно нигде не зафиксированная. | Исход **обрабатываемый** (тот же путь `max_tokens`-blocked, что у TD-018; кредит не списывается); при дефолтном `DEEP_THINKING_EFFORT=medium` режим стабильно отдаёт ответы, поэтому это не дефект. Минимальная дельта спринта 2: не вводить отдельный лимит вывода на deep_thinking-пути. | При `effort=high` (или дальнейшем росте reasoning-нагрузки) на длинных запросах вероятность `blocked(max_tokens)` выше, чем на `smart`-пути — reasoning «крадёт» бюджет ответа. Не утечка, не двойной биллинг; деградация UX (повтор/сокращение запроса). | Митигация — **отдельный output-бюджет на deep_thinking-пути**: новый env `DEEP_THINKING_MAX_OUTPUT_TOKENS` (больше базового `OPENAI_MAX_TOKENS`, покрывает reasoning + видимый ответ), передаваемый как `max_output_tokens` только когда `dialog_mode=deep_thinking`; **либо** повышенный `max_output_tokens` на этом пути по формуле «база + запас на reasoning от effort». Решение фиксируется обновлением [02-tech-stack.md](02-tech-stack.md)/[07-deployment.md](07-deployment.md) и расширением [ADR-059 §4](adr/ADR-059-openai-default-provider.md). Общий переход на streaming (TD-018) также ослабит проблему (частичная отдача). |

| TD-035 | **Redaction audit-payload — денилист по ИМЕНАМ ключей, без guard на свободный текст под несенситивным ключом** ([ADR-056](adr/ADR-056-temporary-chat.md), [05-security.md §Логирование](05-security.md#логирование-безопасное)). `assert_no_secrets`/`redact` (`src/app/observability/redaction.py`) редактируют значения по денилисту **имён** ключей (`key`/`token`/`secret`/`password`/`authorization`/`credential` + exact-список) и `attachments[].data`. **Свободный текст под несенситивным ключом (`message`/`content`/`text`/`prompt`/`question`) НЕ редактируется.** Сейчас утечки нет — только потому, что оркестратор нигде не кладёт текст диалога в audit-payload (audit несёт лишь метаданные: role/model/usage/id — [ADR-056 §4](adr/ADR-056-temporary-chat.md)). Технического guard, который бы это гарантировал, нет. | Денилист по именам прост и покрывает известные секрет-поля; на текущих audit-путях свободный текст диалога не логируется по построению (не по guard). Defense-in-depth против будущей неосторожной правки — отдельная задача. | **Спринты 4–5 добавляют audit-пути** (`quiz.generate` — вопрос/варианты квиза; `image.generate` — `prompt` изображения): неосторожная правка, положившая `question`/`prompt`/текст ответа в audit-payload, **залогирует пользовательский контент**, и redaction это не остановит (ключи несенситивные). Не текущая утечка — латентный риск. | Defense-in-depth: (а) **allowlist полей** audit-payload для chat-события (пропускать только известные метаданные-ключи, прочее — отбросить/REDACTED), **либо** (б) guard, отклоняющий/редактирующий значения-строки длиннее порога под chat-audit-ключами. Зафиксировать в [05-security.md §Логирование](05-security.md#логирование-безопасное). Реализация — backend при добавлении audit-путей квиза/изображений (спринты 4–5). |
| TD-036 | **Физическое удаление истёкших изображений временного чата — best-effort без планировщика** ([ADR-058 §6](adr/ADR-058-image-generation.md), решение пользователя; связано [TD-010](100-known-tech-debt.md)/[TD-013](100-known-tech-debt.md) — нет cron/планировщика). Логическая недоступность истёкшего изображения (`GET /v1/images/{id}` → `404` по `expires_at`) **гарантирована безусловно** (проверка в запросе, не зависит от GC). Но **физическое** удаление байт делается **opportunistic sweep** (малые партии `DELETE ... WHERE expires_at <= now()` на request-путях, под Redis-локом + троттлингом) — на инстансе с низким/нулевым image-трафиком истёкшие (уже нечитаемые) байты могут оставаться в БД до появления трафика или ручного прогона compose-задачи. | Планировщика в проекте нет ([TD-010](100-known-tech-debt.md)/[TD-013](100-known-tech-debt.md)); гарантированный периодический GC потребовал бы новой инфраструктуры (cron/worker). Opportunistic sweep даёт регулярную уборку от трафика без неё; логическая приватность и так безусловна. | Приватность НЕ страдает (истёкшее изображение нечитаемо по `expires_at` независимо от удаления). Остаётся возможный рост БД: истёкшие байты на «тихом» инстансе не выметаются автоматически. Ограничено платностью генерации (`IMAGE_CREDITS_COST`) и TTL. | При появлении планировщика/worker-инфраструктуры — гарантированный периодический sweeper (`DELETE ... WHERE expires_at IS NOT NULL AND expires_at <= now()` партиями). Промежуточно — опциональная one-shot compose-задача рядом с `migrate` для операторской форс-очистки ([ADR-058 §6](adr/ADR-058-image-generation.md), вариант B). Симметрично [TD-031](100-known-tech-debt.md) (object storage). |

| TD-037 | **`TRUSTED_PROXY_IPS=172.18.0.0/16` на сервере novirell доверяет всей подсети `mas-net`, а не только `/32` реального `mas-nginx`** (принято в [ADR-060 §9](adr/ADR-060-novirell-shared-nginx-shared-network-deploy.md), [07-deployment.md §Сервер novirell](07-deployment.md#сервер-novirell-4912189477--nginx-edge-вместо-traefik), [05-security.md §Доверенный reverse-proxy](05-security.md)). После фикса XFF-спуфинга на публичном edge (nginx перезаписывает `X-Forwarded-For` на `$remote_addr`) остаётся вторичный вектор: любой ДРУГОЙ контейнер в общей `mas-net` (напр. скомпрометированный контейнер mail-aggregator) может обратиться к `novirell-api-1:8000` **напрямую в обход nginx**; его peer-IP попадает в доверенную `/16`, поэтому его `X-Forwarded-For` будет принят как честный → `client_ip` спуфится для in-network-атакующего (обход per-IP rate limit `AUTH_RATE_LIMIT_PER_IP`/`ADMIN_RATE_LIMIT_PER_MIN`). | `/32`-пин отвергнут: IP `mas-nginx` (сейчас `172.18.0.7`) **не закреплён** и меняется при пересоздании контейнера. При смене IP peer перестанет матчить `/32` → `_is_trusted_proxy(peer)=false` → `client_ip` (`src/app/deps.py`) вернёт socket peer (= `mas-nginx`) → per-IP лимит станет глобальным — **тихая** деградация (`trusted_proxy_networks()` в `config.py` не падает, а молча перестаёт доверять). Широкая `/16` устойчива к смене IP nginx. | Ограничено in-mas-net атакующим (публичный путь уже защищён overwrite XFF на edge). Не утечка данных, не двойной биллинг; риск — обход rate-limit контейнером внутри общей docker-сети. На MVP приемлемо (в `mas-net` — только доверенный mail-aggregator + наш стек). | При появлении требования строгой изоляции: (а) закрепить статический IP `mas-nginx` в `mas-net` и сузить `TRUSTED_PROXY_IPS` до его `/32`; **либо** (б) вынести наш `api` в выделенную сеть только с nginx (не общую `mas-net`), оставив `mas-net` лишь для inbound от nginx; **либо** (в) сетевой policy/firewall, запрещающий контейнер-контейнер трафик к `novirell-api-1:8000` мимо nginx. Любой вариант — отдельное решение (затрагивает чужой mail-aggregator-стек). |

| TD-038 | **Подключение `novirell-api-1` к общей сети `mas-net` — императивный шаг вне compose** (принято в [ADR-060 §5](adr/ADR-060-novirell-shared-nginx-shared-network-deploy.md), [07-deployment.md §Коллизия имён](07-deployment.md#коллизия-имён-в-shared-external-network-mas-net)). `docker-compose.novirell.yml` отвязывает `api` от edge-сети (иначе compose добавил бы generic-алиас `api` на `mas-net`, перехватывая имя у `mas-api` соседа — COLLISION 2), а подключение к `mas-net` делается `docker network connect --alias novirell-api mas-net novirell-api-1` в деплой-workflow (после `up`, идемпотентно). Атрибут «на mas-net без алиаса `api`» **не декларативен** в compose — держится только на этом шаге. | Compose **всегда** добавляет имя сервиса (`api`) сетевым алиасом и подавить это нельзя (проверено, compose 5.2.0); единственный способ попасть в `mas-net` без алиаса `api` — императивный `docker network connect` (имя сервиса не добавляет). Переименование сервиса `api` отвергнуто (ломает `<proj>-api-1`/readiness-gate на всех инстансах и всё равно утекает своим именем-алиасом). | Шаг обязан выполняться **каждый** деплой (`up` может пересоздать контейнер отвязанным). Пропуск → `api` не в `mas-net` → `mas-nginx` не достаёт upstream → `novirell.shop` отдаёт `502`. Это **fail-safe** (потеря доступности novirell, НЕ перехват чужого трафика и не утечка), но операционно хрупко: доступность зависит от imperative-шага. Workflow гвардит наличие `mas-net` и отсутствие алиаса `api`; ручной bring-up обязан включать connect-шаг (runbook). Кратковременный `502`-blip при пересоздании контейнера до connect. | При появлении декларативного способа (а) выделенная сеть только для `nginx`↔`api` вместо общей `mas-net` — но требует подключить чужой `mas-nginx` к нашей сети (правка стека mail-aggregator, вне нашего контроля); (б) поддержка compose подавления service-name-алиаса, если появится в будущих версиях; (в) оператор/оркестратор (systemd unit / k8s), декларативно закрепляющий network attachment. Любой — отдельное решение; на MVP императивный шаг в workflow + runbook приемлем. Связан с [TD-037](100-known-tech-debt.md) (та же общая `mas-net`, вектор изоляции). |
| TD-039 | **`STOREKIT_TRUST_ANY_XCODE_CERT=true` доверяет ЛЮБОМУ самоподписанному серту с Xcode-CN** (принято в [ADR-061](adr/ADR-061-storekit-trust-any-xcode-cert.md)). На ES256-пути `_verify_real_transaction` (`src/app/subscription/storekit.py`) при флаге `true` И subject CN листа (`chain[0]`) == `"StoreKit Testing in Xcode"` пропускаются оба гейта заякоривания (`if not self._roots` и `_verify_chain` до Apple root CA); ES256-подпись листа при этом проверяется штатно (`jwt.decode(key=leaf_pubkey, algorithms=["ES256"])`), то есть payload не подделать без ключа предъявленного серта, но **доверие к происхождению серта снимается** — любая машина Xcode (любой самоподписанный серт с этим CN) проходит верификацию. Дефолт `false` → прод/остальные инстансы fail-closed, не затронуты. **Только для пред-релизного тестового инстанса** (приложение не в проде, реальных пользователей нет; владелец осознанно принял риск ради сквозного прогона активации подписки + начисления кредитов с реальным StoreKit Testing-потоком iOS). | Реальная Apple sandbox/prod-интеграция и Apple root CA недоступны ([Q-007-1](99-open-questions.md) не закрыт), а локальный StoreKit Testing Xcode отдаёт настоящий ES256-JWS с самоподписанным сертом (не HS256, поэтому `STOREKIT_TEST_MODE`/[TD-007](100-known-tech-debt.md) не покрывает), причём серт **свой на каждой машине** — загрузить его в `APPSTORE_ROOT_CERT_DIR` пришлось бы для каждого разработчика/тестировщика. Один флаг покрывает произвольную машину. При `true` — WARNING в лог на старте. | При случайном `true` в проде — приём подписи любым самоподписанным сертом с фиксированным Xcode-CN (снят anchor-гейт). Ограничено: ES256-подпись листа всё ещё обязательна (внутренняя целостность токена сохраняется); дефолт `false`; отдельный флаг, не влияет на реальную ветку для не-Xcode-CN и на HS256 test-mode. Риск-класс — обход доверия к происхождению серта, НЕ утечка данных/двойной биллинг. | **ОТКЛЮЧИТЬ перед продакшеном** (`STOREKIT_TRUST_ANY_XCODE_CERT=false` + сконфигурировать Apple root CA), как часть [prod-readiness-чек-листа](07-deployment.md#prod-readiness-checklist-must-configure-before-launch) — пункт «staging-режимы выключить». Полное закрытие — при интеграции реальной App Store Server API sandbox/prod ([Q-007-1](99-open-questions.md)), после чего флаг и ветку удалить. |

Пусто по остальным пунктам. Новый долг добавляется сюда с cross-ref из кода/документации.
